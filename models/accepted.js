class accepted{
    constructor(acc_id, interp_id, book_id, acc_date){
        this.accepted_id = acc_id;
        this.interpreter_id = interp_id;
        this.booking_id = book_id;
        this.accepted_date = acc_date;
    }

}
export default accepted; // allows use in other filess