<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Select Your Role</title>
    <link rel="stylesheet" href="/css/styles2.css">
    <!-- Font Awesome CDN -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
            color: #333;
        }

        .main-content {
            margin-top: 60px; /* Adjust based on the height of your header */
        }
        .container {
            width: 100%;
            max-width: 600px;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
            border-radius: 12px;
            text-align: center;
            position: relative;
            margin-top: 80px; /* Add margin to avoid overlap with the navbar */
        }
        .container h2 {
            font-size: 24px;
            color: #333;
            margin-bottom: 40px;
            letter-spacing: 1px;
        }
        .button {
            padding: 15px 30px;
            margin: 10px;
            font-size: 18px;
            color: white;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
        }
        .button:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        }
        .button-teacher {
            background-color: #007bff;
        }
        .button-student {
            background-color: #28a745;
        }
        .button-interpreter {
            background-color: #17a2b8;
        }
    </style>
</head>
<body>
    <%- include('partials/nav') %>
    <div class="main-content">
    <section id="hero">
        <div class="overlay"></div>
        <div class = "container">
        <section id = "services1">
            <section class="features1">
                <h2>Login Areas</h2>
                <div class="feature-grid1">
                    <div class="feature-item1">
                        <img src="/images/interpreter.png" alt="Schedule Icon">
                        <button class="button button-interpreter" onclick="window.location.href='/interpreter/login'">Interpreter</button>
                    </div>
                    <div class="feature-item1">
                        <img src="/images/lecture.png" alt="Lecturer Icon">
                        <button class="button button-teacher" onclick="window.location.href='/lecturer/login'">Lecturer</button>
                    </div>
                    <div class="feature-item1">
                        <img src="/images/graduated.png" alt="Support Icon">
                        <button class="button button-student" onclick="window.location.href='/student/login'">Student</button>
                    </div>
                    <div class="feature-item1">
                        <img src="/images/handshake.png" alt="Support Icon">
                        <button class="button button-admin" onclick="window.location.href='/lecturer/login'">Admin</button>
                    </div>
                </div>
            </section>
        </div>
        </section>
        </div> 
    </section>
</body>
<script>
    const images = [
            '/images/DUT.png',
            '/images/_ASL for “LOVE” - fingerspelling_ Sticker for Sale by Jennaviveart.jpeg',
            '/images/Disability-DSC00483.jpg',
            '/images/DUT-Deaf-Centre.jpg'
        ];

    let currentIndex = 0;
    function changeBackground() {
        const heroSection = document.getElementById('hero');

        // Change the background image
        heroSection.style.backgroundImage = `url(${images[currentIndex]})`;

        // Update the index, and loop back to the first image if needed
        currentIndex = (currentIndex + 1) % images.length;
    }
    setInterval(changeBackground, 5000);
    // Start with the first background image
    changeBackground();
</script>
</html>
